{
  "name": "hhs-room-found-myself",
  "version": "1.0.0",
  "description": "HH's ROOM: Found Myself — personal website with backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "export:static": "node scripts/export-static.js",
    "sync:pages": "npm run export:static && git add public/site-data-static.json public/uploads && (git diff --cached --quiet && echo \"No public changes to sync\" || git commit -m \"Update public site content\") && git push"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^1.4.5-lts.2"
  }
}
